# Optional configuration for run-compose.sh
# Uncomment and set PODMAN_BASE to override the per-stack default.
# Passwords must never be stored here — set them in your shell or
# enter them at the prompt.
#
# PODMAN_BASE="${HOME}/opt/oracle"
